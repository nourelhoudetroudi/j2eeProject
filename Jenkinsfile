pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Ajoute ici tes étapes de build
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Ajoute ici tes étapes de tests
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Ajoute ici tes étapes de déploiement
            }
        }
    }
}

